import { getTimezoneOffset } from 'helpers/getTimezoneOffset';

export const ONE_MINUTE_TIME = 60_000; // 60 * 1000
export const ONE_MINUTE_SECONDS = 60; // 60
export const TIMEZONE_OFFSET = getTimezoneOffset();
